// --- offsets
offsets = {
    //global structures
    entity_list_selector_list_ptr = 0x0399bd60L;

    //controller structure
    ctrl_name = 0x7b8L;
    ctrl_play_index = 0x784L;

    //player_ent structure
    play_hp = 0x9fcL;
    play_pos = 0x127cL;
    play_ap = 0x1460L;

    //view
    view_angles_source_ptr = 0x0386a890L;
    view_angles = 0x508L;
};

// --- settings
settings = {
    //player name
    lain_name = "[72.58 FM] lain"

    //screen resolution
    scr_width = 2560;
    scr_height = 1440;

    //radar mod
    radar_pos_x = 35;                   //top left of radar, in px, x
    radar_pos_y = 35;                   //top left of radar, in px, y
    radar_diameter = 331.0;             //in px
    radar_view_distance_limit = 1865.0; //from center of radar to edge, in world coords (radar zoom = 0.5)
    radar_height_diff_limit = 300.0;    //will take a paragraph to explain, dw about it
    radar_blip_size = 10.0;             //size of blips on radar, in px
    radar_coords_in_pixel = 12.10;       //how many world coords it takes to move 1 px in the radar (radar zoom = 0.5)
}
